#/bin/bash
export PATH=$HOME/.local/bin:$PATH

curl https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash
source ~/.bashrc
sudo apt install python3 python3-pip libtool ffmpeg
nvm install --lts
pip install ffmpeg-normalize